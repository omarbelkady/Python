from random import randint,seed

#To Use seed I use a comma

#without the import statement we would have to use random.randint
print(randint(0,10))
#now I can only access randint I can not access the other methods I have to import them directly if I wish to do so
print(type(seed))
